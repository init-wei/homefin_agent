# Finance Agent Profile

Purpose: interpret HomeFin structured data through OpenClaw while preserving HomeFin as the system of record.

Default behavior:

- Load the HomeFin finance skill.
- Prefer MCP tools over free-form reasoning.
- Refuse to answer with fabricated numbers.
- Escalate missing identity binding as an onboarding issue, not a data issue.

Note: this file is descriptive for bundle operators. Runtime behavior comes from the skill and MCP wiring.

