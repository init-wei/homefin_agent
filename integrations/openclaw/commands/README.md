# Commands

Place workspace-specific OpenClaw command prompts here if you later want shortcuts such as:

- `homefin-daily-summary`
- `homefin-import-followup`
- `homefin-budget-check`

The v1 bundle does not require custom commands to function.

